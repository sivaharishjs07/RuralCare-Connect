'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';
import {
  AlertCircle,
  ArrowRight,
  CalendarDays,
  CheckCircle2,
  ClipboardList,
  Filter,
  Hospital,
  Loader2,
  Plus,
  Search,
  ShieldAlert,
  Stethoscope,
  UserRound,
  X,
} from 'lucide-react';
import { useAuth } from '@/hooks/use-auth';
import { isHealthcareStaff } from '@/lib/auth/roles';
import { supabaseClient } from '@/lib/supabase/client';
import type { Facility, FollowUp, Patient, Profile, Referral } from '@/lib/types/database';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

type FollowUpStatus = Exclude<FollowUp['status'], null>;
type StatusFilter = 'all' | FollowUpStatus;
type FollowUpForm = { patientId: string; referralId: string; facilityId: string; assignedTo: string; scheduledDate: string; status: FollowUpStatus; notes: string; outcome: string };

const emptyForm: FollowUpForm = { patientId: '', referralId: '', facilityId: '', assignedTo: '', scheduledDate: '', status: 'scheduled', notes: '', outcome: '' };
const statusOptions: { value: FollowUpStatus; label: string }[] = [
  { value: 'scheduled', label: 'Scheduled' },
  { value: 'completed', label: 'Completed' },
  { value: 'missed', label: 'Missed' },
  { value: 'cancelled', label: 'Cancelled' },
];

function statusLabel(status: string | null) { return statusOptions.find((option) => option.value === status)?.label ?? 'Not recorded'; }
function statusClasses(status: string | null) {
  if (status === 'completed') return 'border-success/30 bg-success/10 text-success';
  if (status === 'missed' || status === 'cancelled') return 'border-destructive/25 bg-destructive/5 text-destructive';
  return 'border-primary/20 bg-primary/5 text-primary';
}
function formatDate(value: string | null) { if (!value) return 'Not scheduled'; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' }); }
function formatDateTime(value: string | null) { if (!value) return 'Not recorded'; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString(undefined, { dateStyle: 'medium', timeStyle: 'short' }); }

function FollowUpForm({ patients, referrals, facilities, staff, form, saving, error, onChange, onSubmit, onCancel }: { patients: Patient[]; referrals: Referral[]; facilities: Facility[]; staff: Profile[]; form: FollowUpForm; saving: boolean; error: string | null; onChange: (field: keyof FollowUpForm, value: string) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void; onCancel: () => void }) {
  const patientReferrals = referrals.filter((referral) => referral.patient_id === form.patientId);
  const selectClass = 'flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-ring';
  const textareaClass = 'flex min-h-24 w-full rounded-md border border-input bg-background px-3 py-2 text-sm placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring';
  return <Card className="border-primary/20 shadow-sm"><CardHeader className="flex-row items-start justify-between space-y-0 border-b border-border/70"><div><CardTitle className="text-lg">New follow-up</CardTitle><CardDescription className="mt-1">Plan continuity of care using existing records.</CardDescription></div><Button type="button" variant="ghost" size="icon" onClick={onCancel} aria-label="Close follow-up form"><X className="h-4 w-4" /></Button></CardHeader><form onSubmit={onSubmit}><CardContent className="space-y-5 pt-6">{error && <div className="flex items-start gap-2 rounded-md border border-destructive/20 bg-destructive/5 p-3 text-sm text-destructive" role="alert"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0" /><span>{error}</span></div>}<div className="space-y-2"><Label htmlFor="follow-up-patient">Patient <span className="text-destructive">*</span></Label><select id="follow-up-patient" required value={form.patientId} onChange={(event) => onChange('patientId', event.target.value)} className={selectClass}><option value="">Select an existing patient</option>{patients.map((patient) => <option key={patient.id} value={patient.id}>{patient.full_name}</option>)}</select></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="follow-up-referral">Related referral</Label><select id="follow-up-referral" value={form.referralId} onChange={(event) => onChange('referralId', event.target.value)} className={selectClass}><option value="">No related referral</option>{patientReferrals.map((referral) => <option key={referral.id} value={referral.id}>{referral.reason.slice(0, 55)} ({referral.status ?? 'unrecorded'})</option>)}</select></div><div className="space-y-2"><Label htmlFor="follow-up-facility">Facility</Label><select id="follow-up-facility" value={form.facilityId} onChange={(event) => onChange('facilityId', event.target.value)} className={selectClass}><option value="">Not assigned</option>{facilities.map((facility) => <option key={facility.id} value={facility.id}>{facility.name}</option>)}</select></div><div className="space-y-2"><Label htmlFor="follow-up-assignee">Assigned healthcare worker</Label><select id="follow-up-assignee" value={form.assignedTo} onChange={(event) => onChange('assignedTo', event.target.value)} className={selectClass}><option value="">Not assigned</option>{staff.map((person) => <option key={person.id} value={person.id}>{person.full_name}</option>)}</select></div><div className="space-y-2"><Label htmlFor="follow-up-date">Follow-up date <span className="text-destructive">*</span></Label><Input id="follow-up-date" required type="date" value={form.scheduledDate} onChange={(event) => onChange('scheduledDate', event.target.value)} /></div><div className="space-y-2 sm:col-span-2"><Label htmlFor="follow-up-status">Status</Label><select id="follow-up-status" value={form.status} onChange={(event) => onChange('status', event.target.value)} className={selectClass}>{statusOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></div></div><div className="space-y-2"><Label htmlFor="follow-up-notes">Notes / details</Label><textarea id="follow-up-notes" value={form.notes} onChange={(event) => onChange('notes', event.target.value)} rows={3} className={textareaClass} /></div><div className="space-y-2"><Label htmlFor="follow-up-outcome">Outcome</Label><textarea id="follow-up-outcome" value={form.outcome} onChange={(event) => onChange('outcome', event.target.value)} rows={3} className={textareaClass} placeholder="Record an outcome only when one is known" /></div><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button type="submit" disabled={saving}>{saving && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}Create follow-up</Button></div></CardContent></form></Card>;
}

function FollowUpCard({ followUp, patientName, referral, facilityName, assigneeName, canManage, updating, onStatusChange }: { followUp: FollowUp; patientName: string; referral?: Referral; facilityName: string; assigneeName: string; canManage: boolean; updating: boolean; onStatusChange: (followUp: FollowUp, status: FollowUpStatus) => void }) {
  return <Card className="transition-shadow hover:shadow-md"><CardContent className="p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-primary/10 text-primary"><UserRound className="h-5 w-5" /></div><div><h3 className="font-semibold text-foreground">{patientName}</h3><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><CalendarDays className="h-3.5 w-3.5" />{formatDate(followUp.scheduled_date)}</p></div></div><span className={`inline-flex w-fit rounded-full border px-3 py-1 text-xs font-bold ${statusClasses(followUp.status)}`}>{statusLabel(followUp.status)}</span></div><div className="mt-5 grid gap-4 border-t border-border/70 pt-4 text-sm sm:grid-cols-2"><div className="flex items-start gap-2 text-muted-foreground"><Hospital className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><span>{facilityName}</span></div><div className="flex items-start gap-2 text-muted-foreground"><Stethoscope className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><span>{assigneeName}</span></div><div className="flex items-start gap-2 text-muted-foreground"><ClipboardList className="mt-0.5 h-4 w-4 shrink-0 text-primary" /><span>{referral ? `Referral: ${referral.reason}` : 'No related referral'}</span></div><div><p className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Created</p><p className="mt-1 text-foreground">{formatDateTime(followUp.created_at)}</p></div><div><p className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Notes</p><p className="mt-1 whitespace-pre-wrap text-foreground">{followUp.notes || 'No notes recorded'}</p></div><div><p className="text-xs font-medium uppercase tracking-wide text-muted-foreground">Outcome</p><p className="mt-1 whitespace-pre-wrap text-foreground">{followUp.outcome || 'Not recorded'}</p></div></div>{canManage && <div className="mt-4 flex flex-wrap gap-2 border-t border-border/70 pt-4"><span className="mr-1 self-center text-xs font-medium text-muted-foreground">Update status:</span>{statusOptions.filter((option) => option.value !== followUp.status).map((option) => <Button key={option.value} type="button" variant="outline" size="sm" disabled={updating} onClick={() => onStatusChange(followUp, option.value)}>{updating && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}{option.label}</Button>)}</div>}</CardContent></Card>;
}

export default function FollowUpsPage() {
  const { user, role } = useAuth();
  const [followUps, setFollowUps] = useState<FollowUp[]>([]); const [patients, setPatients] = useState<Patient[]>([]); const [referrals, setReferrals] = useState<Referral[]>([]); const [facilities, setFacilities] = useState<Facility[]>([]); const [staff, setStaff] = useState<Profile[]>([]);
  const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [formError, setFormError] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null); const [formOpen, setFormOpen] = useState(false); const [saving, setSaving] = useState(false); const [updatingId, setUpdatingId] = useState<string | null>(null); const [search, setSearch] = useState(''); const [statusFilter, setStatusFilter] = useState<StatusFilter>('all'); const [dateFilter, setDateFilter] = useState(''); const [assignedFilter, setAssignedFilter] = useState('all'); const [form, setForm] = useState<FollowUpForm>(emptyForm);
  const canManage = isHealthcareStaff(role);
  const loadData = async () => { setLoading(true); setError(null); const [followUpResult, patientResult, referralResult, facilityResult, staffResult] = await Promise.all([supabaseClient.from('follow_ups').select('*').order('scheduled_date', { ascending: true }), supabaseClient.from('patients').select('*').order('full_name', { ascending: true }), supabaseClient.from('referrals').select('*').order('referred_at', { ascending: false }), supabaseClient.from('facilities').select('*').order('name', { ascending: true }), supabaseClient.from('profiles').select('*').in('role', ['health_worker', 'doctor', 'admin']).order('full_name', { ascending: true })]); const coreFailure = followUpResult.error ?? patientResult.error; if (coreFailure) setError(coreFailure.message); else { setFollowUps((followUpResult.data ?? []) as FollowUp[]); setPatients((patientResult.data ?? []) as Patient[]); setReferrals(referralResult.error ? [] : (referralResult.data ?? []) as Referral[]); setFacilities(facilityResult.error ? [] : (facilityResult.data ?? []) as Facility[]); setStaff(staffResult.error ? [] : (staffResult.data ?? []) as Profile[]); } setLoading(false); };
  useEffect(() => { if (user) void loadData(); }, [user]);
  const patientNames = useMemo(() => new Map(patients.map((patient) => [patient.id, patient.full_name])), [patients]); const facilityNames = useMemo(() => new Map(facilities.map((facility) => [facility.id, facility.name])), [facilities]); const staffNames = useMemo(() => new Map(staff.map((person) => [person.id, person.full_name])), [staff]);
  const filteredFollowUps = useMemo(() => followUps.filter((followUp) => { const query = search.trim().toLowerCase(); const patientName = patientNames.get(followUp.patient_id) ?? ''; return (!query || patientName.toLowerCase().includes(query) || followUp.notes?.toLowerCase().includes(query) || followUp.outcome?.toLowerCase().includes(query)) && (statusFilter === 'all' || followUp.status === statusFilter) && (!dateFilter || followUp.scheduled_date === dateFilter) && (assignedFilter === 'all' || followUp.assigned_to === assignedFilter); }), [assignedFilter, dateFilter, followUps, patientNames, search, statusFilter]);
  const updateField = (field: keyof FollowUpForm, value: string) => setForm((current) => ({ ...current, [field]: value })); const resetFilters = () => { setSearch(''); setStatusFilter('all'); setDateFilter(''); setAssignedFilter('all'); };
  const saveFollowUp = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!user || !canManage) return; setSaving(true); setFormError(null); setNotice(null); if (!form.patientId || !form.scheduledDate) { setFormError('Select a patient and follow-up date.'); setSaving(false); return; } const { error: saveError } = await supabaseClient.from('follow_ups').insert({ patient_id: form.patientId, referral_id: form.referralId || null, facility_id: form.facilityId || null, assigned_to: form.assignedTo || null, scheduled_date: form.scheduledDate, status: form.status, notes: form.notes.trim() || null, outcome: form.outcome.trim() || null } as never); if (saveError) setFormError(saveError.message); else { setNotice('Follow-up created successfully.'); setForm(emptyForm); setFormOpen(false); await loadData(); } setSaving(false); };
  const changeStatus = async (followUp: FollowUp, status: FollowUpStatus) => { if (!canManage) return; setUpdatingId(followUp.id); setNotice(null); setError(null); const { error: updateError } = await supabaseClient.from('follow_ups').update({ status } as never).eq('id', followUp.id); if (updateError) setError(`Unable to update follow-up status: ${updateError.message}`); else { setNotice(`Follow-up marked ${statusLabel(status).toLowerCase()}.`); await loadData(); } setUpdatingId(null); };

  return <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8 lg:py-10"><div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-sm font-semibold uppercase tracking-wider text-primary">Continuity of care</p><h1 className="text-3xl font-bold tracking-tight text-foreground">Follow-Up Care</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Plan and track follow-up care after referrals and healthcare interactions.</p></div>{canManage && <Button type="button" onClick={() => { setFormError(null); setNotice(null); setFormOpen(true); }}><Plus className="mr-2 h-4 w-4" />New Follow-Up</Button>}</div>{notice && <div className="mb-6 flex items-center gap-2 rounded-md border border-success/30 bg-success/10 p-3 text-sm text-success" role="status"><CheckCircle2 className="h-4 w-4" />{notice}</div>}{!canManage && <div className="mb-6 flex items-start gap-2 rounded-md border border-primary/20 bg-primary/5 p-3 text-sm text-muted-foreground"><ShieldAlert className="mt-0.5 h-4 w-4 shrink-0 text-primary" />You have read-only follow-up access. Existing Supabase RLS policies remain the final authority.</div>}<div className="grid gap-6 lg:grid-cols-[minmax(0,1fr)_minmax(20rem,25rem)]"><section className="min-w-0 space-y-5"><Card><CardContent className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-4"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search patient or notes" className="pl-9" aria-label="Search follow-ups" /></div><div className="relative"><Filter className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value as StatusFilter)} className="flex h-10 w-full rounded-md border border-input bg-background pl-9 pr-3 text-sm focus:outline-none focus:ring-2 focus:ring-ring" aria-label="Filter follow-ups by status"><option value="all">All statuses</option>{statusOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></div><select value={assignedFilter} onChange={(event) => setAssignedFilter(event.target.value)} className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-ring" aria-label="Filter by assigned worker"><option value="all">All assignees</option>{staff.map((person) => <option key={person.id} value={person.id}>{person.full_name}</option>)}</select><div className="flex gap-2"><Input type="date" value={dateFilter} onChange={(event) => setDateFilter(event.target.value)} aria-label="Filter follow-ups by date" /><Button type="button" variant="ghost" size="icon" onClick={resetFilters} aria-label="Clear follow-up filters"><X className="h-4 w-4" /></Button></div></CardContent></Card>{error && <div className="flex items-start gap-3 rounded-lg border border-destructive/20 bg-destructive/5 p-4 text-sm text-destructive" role="alert"><AlertCircle className="mt-0.5 h-5 w-5 shrink-0" /><div><p className="font-semibold">Unable to load follow-ups</p><p className="mt-1">{error}</p><Button type="button" variant="outline" size="sm" onClick={() => void loadData()} className="mt-3">Try again</Button></div></div>}<div><h2 className="text-lg font-semibold text-foreground">Follow-up list</h2><p className="text-sm text-muted-foreground">{loading ? 'Loading records...' : `${filteredFollowUps.length} follow-up${filteredFollowUps.length === 1 ? '' : 's'} shown`}</p></div>{loading && !error && <div className="flex min-h-48 items-center justify-center rounded-lg border border-border/70 bg-card"><Loader2 className="h-6 w-6 animate-spin text-primary" /><span className="ml-2 text-sm text-muted-foreground">Loading follow-ups...</span></div>}{!loading && !error && filteredFollowUps.length === 0 && <div className="flex min-h-48 flex-col items-center justify-center rounded-lg border border-dashed border-border bg-card px-6 text-center"><ArrowRight className="h-8 w-8 text-muted-foreground/60" /><h3 className="mt-3 font-semibold text-foreground">No follow-ups found</h3><p className="mt-1 text-sm text-muted-foreground">{search || dateFilter || statusFilter !== 'all' || assignedFilter !== 'all' ? 'Try clearing or changing the filters.' : canManage ? 'Create a follow-up to continue a patient care journey.' : 'Follow-ups available to your account will appear here.'}</p></div>}{!loading && !error && filteredFollowUps.length > 0 && <div className="space-y-4">{filteredFollowUps.map((followUp) => <FollowUpCard key={followUp.id} followUp={followUp} patientName={patientNames.get(followUp.patient_id) ?? 'Patient record unavailable'} referral={referrals.find((referral) => referral.id === followUp.referral_id)} facilityName={followUp.facility_id ? facilityNames.get(followUp.facility_id) ?? 'Facility unavailable' : 'Facility not assigned'} assigneeName={followUp.assigned_to ? staffNames.get(followUp.assigned_to) ?? 'Assigned worker unavailable' : 'Not assigned'} canManage={canManage} updating={updatingId === followUp.id} onStatusChange={changeStatus} />)}</div>}</section><aside>{formOpen && canManage && <FollowUpForm patients={patients} referrals={referrals} facilities={facilities} staff={staff} form={form} saving={saving} error={formError} onChange={updateField} onSubmit={saveFollowUp} onCancel={() => { if (!saving) { setFormOpen(false); setFormError(null); } }} />}{!formOpen && canManage && <Card className="hidden border-dashed border-border/80 bg-card/50 lg:block"><CardContent className="flex flex-col items-center px-6 py-10 text-center"><CalendarDays className="h-8 w-8 text-primary/70" /><h2 className="mt-4 font-semibold text-foreground">Plan continuity</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Link a follow-up to an existing referral when the relationship is known.</p><Button type="button" variant="outline" onClick={() => setFormOpen(true)} className="mt-5"><Plus className="mr-2 h-4 w-4" />New Follow-Up</Button></CardContent></Card>}</aside></div></div>;
}